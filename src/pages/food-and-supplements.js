import Food from "../views/food-and-supplements"
import { graphql } from "gatsby"

export default Food

export const query = graphql`
  query ($language: String!) {
    locales: allLocale(filter: { language: { eq: $language } }) {
      edges {
        node {
          ns
          data
          language
        }
      }
    }
  }
`
